src_files:
  - "spec/vendor/**/*.{js,coffee}"
  - "src/**/*.{js,coffee}"

stylesheets:

helpers:
  - "helpers/spec-helper.js"
  - "helpers/**/*.{js,coffee}"

spec_files:
  - "**/*[Ss]pec.{js,coffee}"

src_dir:

spec_dir: spec